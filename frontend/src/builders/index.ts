/** Importing this module registers every preview builder (mirrors
 * blender/builders/__init__.py). */
import "./streetLight";

export { computePrimitives, MATERIAL_PRESETS, specParams } from "./base";
