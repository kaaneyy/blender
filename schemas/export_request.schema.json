{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://assetforge.dev/schemas/export_request.schema.json",
  "title": "ExportRequest",
  "description": "Job payload for the Blender worker: a validated AssetSpec plus output options.",
  "type": "object",
  "additionalProperties": false,
  "required": ["spec", "formats"],
  "properties": {
    "spec": { "$ref": "asset_spec.schema.json" },
    "formats": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "enum": ["dae", "obj", "glb", "fbx"] },
      "description": "dae (Collada) is the SketchUp-native default; obj/glb/fbx for Blender and animation pipelines."
    },
    "lod": {
      "enum": ["high", "medium", "low"],
      "default": "high",
      "description": "Decimation level: high=100%, medium=50%, low=20% of full poly count."
    },
    "poly_budget": {
      "type": "integer",
      "minimum": 100,
      "description": "Optional hard triangle cap; overrides lod percentage when set."
    },
    "thumbnail": {
      "type": "boolean",
      "default": true,
      "description": "Render a 512px turntable PNG alongside the geometry."
    }
  }
}
